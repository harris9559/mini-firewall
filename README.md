# Mini Firewall 🔥

A lightweight Python-based firewall using Scapy with a Streamlit dashboard interface.

## Features
- Live packet sniffing
- Block by IP, port, or protocol
- TCP RST injection for denied connections
- Interactive Streamlit dashboard
- CLI tool for testing

## Run it
```bash
streamlit run dashboard.py
